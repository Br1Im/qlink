# 🚀 Запуск Backend для сохранения данных в БД

## Текущая ситуация

Frontend работает на **http://localhost:3000** и сохраняет данные в **localStorage** браузера.

Для сохранения данных в PostgreSQL базу данных нужно запустить backend.

## Проблема с Prisma

Backend не запускается из-за проблем с загрузкой Prisma engines (сетевые ошибки).

## Решения

### Вариант 1: Использовать VPN

Prisma engines загружаются с серверов, которые могут быть заблокированы. Попробуйте:

1. Включить VPN
2. Запустить команду:
```bash
cd packages/backend
npx prisma generate
```

### Вариант 2: Ручная установка Prisma engines

1. Скачайте Prisma engines вручную с GitHub:
   - https://github.com/prisma/prisma-engines/releases

2. Установите переменную окружения:
```bash
set PRISMA_QUERY_ENGINE_BINARY=C:\path\to\query-engine.exe
```

3. Запустите генерацию:
```bash
cd packages/backend
npx prisma generate
```

### Вариант 3: Использовать текущую версию (localStorage)

Приложение полностью функционально с localStorage:
- ✅ Все данные сохраняются локально
- ✅ Не пропадают после перезагрузки
- ✅ Работает без интернета
- ⚠️ Данные хранятся только в браузере
- ⚠️ Не синхронизируются между устройствами

## После успешной установки Prisma

1. Запустите backend:
```bash
npm run dev:backend
```

2. Backend запустится на **http://localhost:4000**

3. Желтое уведомление "Backend API недоступен" исчезнет

4. Данные начнут сохраняться в PostgreSQL

## Проверка статуса

- **Frontend**: http://localhost:3000 ✅ Работает
- **PostgreSQL**: порт 5432 ✅ Работает
- **Backend API**: http://localhost:4000 ❌ Не запущен (проблема с Prisma)

## Текущий функционал

Все функции работают с localStorage:
- ✅ Создание/редактирование записей
- ✅ Создание/редактирование клиентов
- ✅ Создание/редактирование сотрудников
- ✅ Сохранение настроек
- ✅ Аналитика и графики

Данные сохраняются в браузере и не пропадают после перезагрузки.
