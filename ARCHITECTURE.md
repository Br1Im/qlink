# 🏗 Архитектура Qlink

## 📐 Общая архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                         КЛИЕНТЫ                              │
├──────────────────┬──────────────────┬──────────────────────┤
│   Web Browser    │  Telegram Bot    │  Mobile App (TODO)   │
│   (Next.js)      │   (Telegraf)     │  (React Native)      │
└────────┬─────────┴────────┬─────────┴──────────┬───────────┘
         │                  │                    │
         └──────────────────┼────────────────────┘
                            │
                    ┌───────▼────────┐
                    │   API Gateway   │
                    │   (Express)     │
                    └───────┬────────┘
                            │
         ┌──────────────────┼──────────────────┐
         │                  │                  │
    ┌────▼────┐      ┌─────▼─────┐     ┌─────▼─────┐
    │  Auth   │      │ Business  │     │  Booking  │
    │ Service │      │  Service  │     │  Service  │
    └────┬────┘      └─────┬─────┘     └─────┬─────┘
         │                  │                  │
         └──────────────────┼──────────────────┘
                            │
                    ┌───────▼────────┐
                    │   PostgreSQL   │
                    │   (Prisma ORM) │
                    └────────────────┘
```

## 🎯 Технологический стек

### Frontend
```
Next.js 14 (App Router)
├── React 18
├── TypeScript
├── Tailwind CSS
├── Lucide Icons
└── React Hook Form (TODO)
```

### Backend
```
Node.js + Express
├── TypeScript
├── Telegraf (Telegram Bot)
├── Prisma ORM
├── JWT Authentication
├── Bcrypt (TODO)
└── Node-cron (TODO)
```

### Database
```
PostgreSQL 15
├── 14 таблиц
├── 7 Enums
├── Индексы
└── Связи (Foreign Keys)
```

### Infrastructure
```
Docker Compose
├── PostgreSQL
├── Redis (TODO)
└── Nginx (TODO)
```

## 📁 Структура проекта

```
Qlink/
├── packages/
│   ├── frontend/                    # Next.js приложение
│   │   ├── src/
│   │   │   ├── app/                # App Router
│   │   │   │   ├── (dashboard)/   # Группа роутов
│   │   │   │   │   ├── layout.tsx # Layout с sidebar
│   │   │   │   │   └── dashboard/
│   │   │   │   │       ├── page.tsx
│   │   │   │   │       ├── bookings/
│   │   │   │   │       ├── clients/
│   │   │   │   │       ├── services/
│   │   │   │   │       ├── staff/
│   │   │   │   │       ├── analytics/
│   │   │   │   │       └── settings/
│   │   │   │   ├── map/
│   │   │   │   ├── register/
│   │   │   │   ├── login/
│   │   │   │   ├── layout.tsx     # Root layout
│   │   │   │   └── page.tsx       # Лендинг
│   │   │   ├── components/        # Компоненты
│   │   │   │   ├── AnimateOnScroll.tsx
│   │   │   │   ├── ScrollReveal.tsx
│   │   │   │   ├── BusinessCard.tsx
│   │   │   │   └── BusinessCardsContext.tsx
│   │   │   └── lib/               # Утилиты (TODO)
│   │   ├── public/                # Статика
│   │   ├── package.json
│   │   ├── tsconfig.json
│   │   └── tailwind.config.ts
│   │
│   └── backend/                    # Node.js бэкенд
│       ├── src/
│       │   ├── api/               # REST API
│       │   │   ├── routes/
│       │   │   │   ├── auth.ts
│       │   │   │   ├── business.ts
│       │   │   │   ├── bookings.ts
│       │   │   │   ├── services.ts
│       │   │   │   ├── staff.ts
│       │   │   │   └── reviews.ts
│       │   │   ├── middleware/
│       │   │   │   └── auth.ts
│       │   │   └── index.ts
│       │   ├── bot/               # Telegram Bot
│       │   │   ├── index.ts
│       │   │   ├── start.ts
│       │   │   ├── handlers/      # (TODO)
│       │   │   ├── keyboards/     # (TODO)
│       │   │   └── utils/         # (TODO)
│       │   └── services/          # Бизнес-логика (TODO)
│       ├── prisma/
│       │   ├── schema.prisma
│       │   ├── seed.ts
│       │   └── migrations/
│       ├── .env
│       ├── package.json
│       └── tsconfig.json
│
├── docker-compose.yml
├── README.md
├── QUICKSTART.md
├── FEATURES.md
├── TODO.md
└── ARCHITECTURE.md
```

## 🗄 Схема базы данных

```
┌─────────────┐         ┌──────────────┐
│    User     │         │BusinessOwner │
│ (Клиенты)   │         │ (Владельцы)  │
└──────┬──────┘         └──────┬───────┘
       │                       │
       │                       │ owns
       │                       ▼
       │                ┌─────────────┐
       │                │  Business   │
       │                │ (Заведения) │
       │                └──────┬──────┘
       │                       │
       │         ┌─────────────┼─────────────┐
       │         │             │             │
       │    ┌────▼────┐   ┌───▼────┐   ┌───▼────┐
       │    │  Staff  │   │Service │   │ Review │
       │    └────┬────┘   └───┬────┘   └────────┘
       │         │            │
       │         └────┬───────┘
       │              │
       │         ┌────▼────────┐
       │         │ServiceStaff │
       │         └─────────────┘
       │
       │         ┌─────────────┐
       └────────►│   Booking   │
                 │  (Записи)   │
                 └─────────────┘
```

### Основные таблицы:

**User** (Клиенты)
- id, telegramId, phone, firstName, lastName, email
- Связи: bookings, reviews, favorites, notifications

**BusinessOwner** (Владельцы)
- id, email, phone, firstName, lastName, company
- Связи: businesses

**Business** (Заведения)
- id, ownerId, name, slug, category, address, city
- workingHours (JSON), rating, reviewCount
- Связи: staff, services, bookings, reviews

**Staff** (Сотрудники)
- id, businessId, firstName, lastName, position
- workingHours (JSON), rating
- Связи: services (через ServiceStaff), bookings

**Service** (Услуги)
- id, businessId, name, category, price, duration
- Связи: staff (через ServiceStaff), bookings

**Booking** (Записи)
- id, userId, businessId, serviceId, staffId
- date, startTime, endTime, price, status
- Связи: user, business, service, staff

**Review** (Отзывы)
- id, userId, businessId, rating, comment
- response, responseDate
- Связи: user, business

## 🔐 Аутентификация

```
┌──────────┐                    ┌──────────┐
│  Client  │                    │  Server  │
└────┬─────┘                    └────┬─────┘
     │                               │
     │  POST /api/auth/register      │
     │  { email, password, ... }     │
     ├──────────────────────────────►│
     │                               │
     │         JWT Token             │
     │◄──────────────────────────────┤
     │                               │
     │  GET /api/business            │
     │  Authorization: Bearer <JWT>  │
     ├──────────────────────────────►│
     │                               │
     │  Verify JWT                   │
     │  ├─ Valid? ──► Return data    │
     │  └─ Invalid? ─► 401 Error     │
     │                               │
     │         Response              │
     │◄──────────────────────────────┤
     │                               │
```

### JWT Payload:
```json
{
  "id": "user_id",
  "email": "user@example.com",
  "iat": 1234567890,
  "exp": 1234567890
}
```

## 🔄 Процесс записи

```
┌────────┐     ┌─────────┐     ┌──────────┐     ┌──────────┐
│ Client │     │   Bot   │     │   API    │     │    DB    │
└───┬────┘     └────┬────┘     └────┬─────┘     └────┬─────┘
    │               │               │                │
    │ /start        │               │                │
    ├──────────────►│               │                │
    │               │               │                │
    │ Выбор         │               │                │
    │ категории     │               │                │
    ├──────────────►│               │                │
    │               │               │                │
    │               │ GET /business │                │
    │               ├──────────────►│                │
    │               │               │ SELECT *       │
    │               │               ├───────────────►│
    │               │               │                │
    │               │               │ Results        │
    │               │               │◄───────────────┤
    │               │ Businesses    │                │
    │               │◄──────────────┤                │
    │               │               │                │
    │ Список        │               │                │
    │ заведений     │               │                │
    │◄──────────────┤               │                │
    │               │               │                │
    │ Выбор         │               │                │
    │ заведения     │               │                │
    ├──────────────►│               │                │
    │               │               │                │
    │ Выбор услуги  │               │                │
    ├──────────────►│               │                │
    │               │               │                │
    │ Выбор даты    │               │                │
    ├──────────────►│               │                │
    │               │               │                │
    │ Выбор времени │               │                │
    ├──────────────►│               │                │
    │               │               │                │
    │ Телефон       │               │                │
    ├──────────────►│               │                │
    │               │               │                │
    │               │ POST /bookings│                │
    │               ├──────────────►│                │
    │               │               │ INSERT         │
    │               │               ├───────────────►│
    │               │               │                │
    │               │               │ Success        │
    │               │               │◄───────────────┤
    │               │ Booking       │                │
    │               │◄──────────────┤                │
    │               │               │                │
    │ Подтверждение │               │                │
    │◄──────────────┤               │                │
    │               │               │                │
```

## 🎨 Frontend архитектура

### Структура компонентов:

```
App
├── Layout (Root)
│   ├── Header
│   └── Footer
│
├── Page (Лендинг)
│   ├── Hero
│   ├── Features
│   ├── Categories
│   │   └── BusinessCard (с анимациями)
│   ├── Ecosystem
│   ├── HowItWorks
│   └── CTA
│
├── Map Page
│   ├── Sidebar
│   │   ├── Search
│   │   ├── Categories
│   │   └── Filters
│   └── Results
│       ├── Map
│       └── BusinessList
│
├── Auth Pages
│   ├── Register (Multi-step)
│   └── Login
│
└── Dashboard Layout
    ├── Sidebar
    └── Pages
        ├── Dashboard
        ├── Bookings
        ├── Clients
        ├── Services
        ├── Staff
        ├── Analytics
        └── Settings
```

### State Management (TODO):

```typescript
// Zustand Store
interface AppState {
  user: User | null;
  business: Business | null;
  bookings: Booking[];
  
  // Actions
  setUser: (user: User) => void;
  setBusiness: (business: Business) => void;
  fetchBookings: () => Promise<void>;
}
```

## 🔌 API архитектура

### REST API структура:

```
/api
├── /auth
│   ├── POST /register
│   ├── POST /login
│   ├── POST /refresh
│   └── GET  /me
│
├── /business
│   ├── GET    /
│   ├── GET    /:id
│   ├── POST   /
│   ├── PUT    /:id
│   ├── DELETE /:id
│   ├── GET    /:id/services
│   └── GET    /:id/available-slots
│
├── /bookings
│   ├── POST   /
│   ├── GET    /user/:userId
│   ├── GET    /business/:businessId
│   ├── GET    /:id
│   ├── PATCH  /:id/status
│   ├── DELETE /:id
│   └── PATCH  /:id/reschedule
│
├── /services
│   ├── POST   /
│   ├── PUT    /:id
│   └── DELETE /:id
│
├── /staff
│   ├── POST   /
│   ├── GET    /business/:businessId
│   ├── PUT    /:id
│   └── DELETE /:id
│
└── /reviews
    ├── POST   /
    ├── GET    /business/:businessId
    └── PATCH  /:id/response
```

### Middleware цепочка:

```
Request
  │
  ├─► CORS
  │
  ├─► Body Parser
  │
  ├─► Logger
  │
  ├─► Auth Middleware (для защищенных роутов)
  │   ├─ Verify JWT
  │   ├─ Check permissions
  │   └─ Attach user to request
  │
  ├─► Route Handler
  │   ├─ Validate input
  │   ├─ Business logic
  │   └─ Database query
  │
  ├─► Response
  │
  └─► Error Handler
```

## 🤖 Telegram Bot архитектура

### Структура бота:

```
Bot
├── Commands
│   ├── /start
│   ├── /help
│   └── /settings
│
├── Handlers
│   ├── Text handlers
│   ├── Callback handlers
│   └── Contact handlers
│
├── Keyboards
│   ├── Main menu
│   ├── Categories
│   ├── Services
│   └── Time slots
│
└── Utils
    ├── Formatters
    ├── Validators
    └── Helpers
```

### State Management:

```typescript
// Session data (TODO: Redis)
interface BotSession {
  userId: string;
  step: 'category' | 'business' | 'service' | 'date' | 'time';
  selectedCategory?: string;
  selectedBusiness?: string;
  selectedService?: string;
  selectedDate?: string;
}
```

## 🚀 Deployment

### Development:
```
localhost:3000  → Frontend (Next.js)
localhost:4000  → Backend API (Express)
localhost:5432  → PostgreSQL
localhost:6379  → Redis (TODO)
```

### Production (TODO):
```
Vercel          → Frontend
Railway/Heroku  → Backend API
Railway/Heroku  → PostgreSQL
Upstash         → Redis
Cloudflare      → CDN
```

## 📊 Мониторинг (TODO)

```
Application
  │
  ├─► Logs (Winston)
  │   └─► Elasticsearch
  │
  ├─► Metrics (Prometheus)
  │   └─► Grafana
  │
  ├─► Errors (Sentry)
  │   └─► Alerts
  │
  └─► Analytics (Google Analytics)
      └─► Dashboard
```

## 🔒 Безопасность

### Уровни защиты:

1. **Network Level**
   - HTTPS only
   - CORS настройки
   - Rate limiting

2. **Application Level**
   - JWT authentication
   - Input validation
   - SQL injection protection (Prisma)
   - XSS protection

3. **Database Level**
   - Encrypted connections
   - Row-level security (TODO)
   - Backup strategy

4. **Infrastructure Level**
   - Firewall rules
   - DDoS protection
   - Security updates

## 📈 Масштабирование (TODO)

### Horizontal Scaling:
```
Load Balancer
  │
  ├─► API Server 1
  ├─► API Server 2
  └─► API Server 3
      │
      └─► PostgreSQL (Primary)
          ├─► Replica 1
          └─► Replica 2
```

### Caching Strategy:
```
Request
  │
  ├─► Redis Cache
  │   ├─ Hit? → Return
  │   └─ Miss? ↓
  │
  └─► Database
      └─► Cache result
```

---

**Архитектура спроектирована для масштабирования и поддержки! 🏗️**
