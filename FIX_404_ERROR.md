# Исправление ошибки "Страница не найдена"

## 🔴 Проблема

Вы видите ошибку "Страница не найдена" при попытке открыть приложение.

## ✅ Решение

### Вариант 1: Перезапустить frontend

```bash
# Остановите текущий процесс (Ctrl+C)
# Затем запустите снова:
cd packages/frontend
npm run dev
```

### Вариант 2: Очистить кэш и перезапустить

```bash
cd packages/frontend

# Удалить .next папку
rm -rf .next

# Или на Windows:
# rmdir /s /q .next

# Запустить снова
npm run dev
```

### Вариант 3: Переустановить зависимости

```bash
cd packages/frontend

# Удалить node_modules
rm -rf node_modules

# Или на Windows:
# rmdir /s /q node_modules

# Установить снова
npm install

# Запустить
npm run dev
```

## 🧪 Проверка

После запуска откройте:

1. **Главная страница:** http://localhost:3000
   - Должна показать landing page с кнопками "Войти" и "Зарегистрироваться"

2. **Страница входа:** http://localhost:3000/login
   - Должна показать форму входа

3. **Страница регистрации:** http://localhost:3000/register
   - Должна показать форму регистрации

## 🔍 Диагностика

Если проблема сохраняется, проверьте:

### 1. Порт занят?

```bash
# Проверить что порт 3000 свободен
# Windows:
netstat -ano | findstr :3000

# Linux/Mac:
lsof -i :3000
```

Если порт занят, остановите процесс или используйте другой порт:

```bash
PORT=3001 npm run dev
```

### 2. Ошибки в консоли?

Посмотрите в терминал где запущен frontend. Если есть ошибки компиляции:

```bash
# Проверить TypeScript ошибки
npm run build
```

### 3. Проверить файлы

Убедитесь что существуют:
- `packages/frontend/src/app/page.tsx` ✅
- `packages/frontend/src/app/layout.tsx` ✅
- `packages/frontend/src/app/login/page.tsx` ✅
- `packages/frontend/src/app/register/page.tsx` ✅
- `packages/frontend/src/app/(dashboard)/dashboard/page.tsx` ✅

## 🚀 Быстрый старт с нуля

Если ничего не помогает:

```bash
# 1. Остановить все процессы
# Ctrl+C во всех терминалах

# 2. Очистить все
cd packages/frontend
rm -rf .next node_modules

# 3. Установить зависимости
npm install

# 4. Запустить
npm run dev

# 5. Открыть браузер
# http://localhost:3000
```

## 📝 Что должно работать

### Без backend:
- ✅ Главная страница (/)
- ✅ Страница входа (/login)
- ✅ Страница регистрации (/register)
- ❌ Dashboard (требует backend)

### С backend:
- ✅ Все страницы
- ✅ Аутентификация
- ✅ Сохранение данных
- ✅ Загрузка данных

## 💡 Совет

Всегда проверяйте консоль браузера (F12) и терминал для ошибок!
