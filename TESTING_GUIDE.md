# Руководство по тестированию

## 🧪 Как протестировать изменения

### 1. Без backend (только frontend)

Если вы хотите протестировать frontend без запуска backend:

```bash
cd packages/frontend
npm run dev
```

**Что будет работать:**
- ✅ Главная страница (/)
- ✅ Страница логина (/login)
- ✅ Страница регистрации (/register)
- ✅ Навигация

**Что НЕ будет работать:**
- ❌ Dashboard (требует аутентификации)
- ❌ Сохранение данных
- ❌ Загрузка данных

**Что вы увидите:**
- Предупреждение "Backend API недоступен"
- Пустые данные на dashboard
- Ошибки при попытке сохранения

### 2. С backend (полная функциональность)

#### Шаг 1: Запустить базу данных

```bash
# Через Docker
docker-compose up -d postgres

# Или используйте локальный PostgreSQL
```

#### Шаг 2: Настроить backend

```bash
cd packages/backend

# Создать .env файл
cat > .env << EOF
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/qlink"
JWT_SECRET="your-super-secret-key-change-in-production"
API_PORT=4000
EOF

# Запустить миграции
npx prisma migrate dev

# Заполнить тестовыми данными (опционально)
npx prisma db seed
```

#### Шаг 3: Запустить backend

```bash
cd packages/backend
npm run dev
```

Вы должны увидеть:
```
🚀 API сервер запущен на порту 4000
📍 http://localhost:4000
```

#### Шаг 4: Запустить frontend

```bash
# В новом терминале
cd packages/frontend
npm run dev
```

Откройте http://localhost:3000

### 3. Тестирование функциональности

#### Регистрация нового пользователя

1. Откройте http://localhost:3000/register
2. Заполните форму:
   - Имя: Иван
   - Фамилия: Петров
   - Email: ivan@example.com
   - Телефон: +7 999 123 45 67
   - Пароль: password123
   - Название бизнеса: Мой салон
   - Категория: Красота
   - Город: Москва
3. Нажмите "Зарегистрироваться"
4. Вы будете перенаправлены на dashboard

#### Проверка dashboard

1. После входа вы должны увидеть:
   - Приветственное сообщение
   - Статистику (все нули для нового пользователя)
   - Кнопки для добавления услуг и сотрудников

#### Добавление сотрудника

1. Перейдите в "Сотрудники"
2. Нажмите "Добавить сотрудника"
3. Заполните форму
4. Нажмите "Сохранить"
5. Сотрудник должен появиться в списке

#### Добавление услуги

1. Перейдите в "Услуги"
2. Нажмите "Добавить услугу"
3. Заполните форму
4. Нажмите "Сохранить"
5. Услуга должна появиться в списке

### 4. Проверка что localStorage НЕ используется

#### Откройте DevTools (F12)

1. Перейдите на вкладку "Application" (Chrome) или "Storage" (Firefox)
2. Откройте "Local Storage" → "http://localhost:3000"
3. Вы должны увидеть ТОЛЬКО:
   - `qlink-ui-settings` (тема и язык - это OK)

**НЕ должно быть:**
- ❌ `demo-mode`
- ❌ `demo-type`
- ❌ `qlink-staff`
- ❌ `qlink-services`
- ❌ `qlink-bookings`
- ❌ `qlink-clients`
- ❌ `qlink-business-data`
- ❌ `qlink-profile-data`

#### Проверка Cookies

1. В DevTools перейдите на "Cookies"
2. Вы должны увидеть:
   - ✅ `qlink_auth_token` (после входа)

### 5. Проверка работы без backend

1. Остановите backend (Ctrl+C)
2. Обновите страницу dashboard
3. Вы должны увидеть:
   - ⚠️ Предупреждение "Backend API недоступен"
   - Пустые данные (нули в статистике)
   - Пустые списки сотрудников/услуг
   - Кнопки работают, но показывают ошибки

### 6. Проверка плавных переходов

1. Перемещайтесь между разделами:
   - Главная → Записи → Клиенты → Услуги → Сотрудники
2. Все переходы должны быть плавными (0.3s)
3. Hover эффекты на кнопках и карточках
4. Анимации при открытии модальных окон

### 7. Проверка Toast уведомлений

1. Попробуйте войти с неверными данными
2. Должно появиться красное уведомление с ошибкой
3. Успешный вход должен показать зеленое уведомление
4. Уведомления должны автоматически исчезать через 5 секунд

## 🐛 Частые проблемы

### "Страница не найдена"

**Причина:** Frontend не запущен или неверный URL

**Решение:**
```bash
cd packages/frontend
npm run dev
```
Откройте http://localhost:3000

### "Backend API недоступен"

**Причина:** Backend не запущен

**Решение:**
```bash
cd packages/backend
npm run dev
```

### "Ошибка подключения к БД"

**Причина:** PostgreSQL не запущен

**Решение:**
```bash
docker-compose up -d postgres
```

### "Unauthorized" при запросах

**Причина:** Токен не сохранен или истек

**Решение:**
1. Выйдите из системы
2. Войдите снова
3. Проверьте cookies в DevTools

### Данные не сохраняются

**Причина:** Backend API не работает или ошибка в коде

**Решение:**
1. Проверьте логи backend
2. Проверьте Network tab в DevTools
3. Убедитесь что БД запущена

## ✅ Чеклист тестирования

- [ ] Frontend запускается без ошибок
- [ ] Backend запускается без ошибок
- [ ] База данных подключена
- [ ] Регистрация работает
- [ ] Вход работает
- [ ] Dashboard загружается
- [ ] Можно добавить сотрудника
- [ ] Можно добавить услугу
- [ ] Можно добавить клиента
- [ ] Можно создать запись
- [ ] Данные сохраняются в БД
- [ ] localStorage НЕ используется для данных
- [ ] Cookies используются для токенов
- [ ] Плавные переходы работают
- [ ] Toast уведомления работают
- [ ] Кнопки реагируют на нажатия
- [ ] Hover эффекты работают
- [ ] При недоступности backend показывается предупреждение
- [ ] При недоступности backend данные НЕ отображаются

## 🎉 Готово!

Если все пункты чеклиста выполнены - приложение работает корректно!
