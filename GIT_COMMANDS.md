# 📦 Git команды для деплоя

## Подготовка к коммиту

```bash
# Проверить статус
git status

# Добавить все изменения
git add .

# Или добавить конкретные файлы
git add packages/frontend/src/
git add packages/backend/src/
git add DEPLOY_TO_HOSTING.md
```

## Коммит изменений

```bash
git commit -m "feat: полное удаление localStorage, плавные переходы, улучшения UI

- Удалено использование localStorage для данных
- Токены теперь в HTTP-only cookies
- Добавлены плавные переходы и анимации
- Создан универсальный компонент Button
- Добавлена система Toast уведомлений
- Улучшены модальные окна
- Добавлен middleware для защиты маршрутов
- Созданы backend роуты для dashboard
- Все данные теперь только из БД через API
- Добавлена документация для деплоя"
```

## Отправка на GitHub

```bash
# Если репозиторий еще не создан
git remote add origin https://github.com/your-username/qlink.git

# Отправить изменения
git push -u origin main

# Или если ветка называется master
git push -u origin master
```

## На хостинге

```bash
# 1. Клонировать репозиторий (первый раз)
cd /var/www
git clone https://github.com/your-username/qlink.git
cd qlink

# 2. Или обновить существующий
cd /var/www/qlink
git pull origin main

# 3. Создать .env файл (см. DEPLOY_TO_HOSTING.md)
nano .env

# 4. Запустить через Docker
docker-compose down
docker-compose up -d

# 5. Запустить миграции
docker exec -it qlink-backend sh
cd packages/backend
npx prisma migrate deploy
exit

# 6. Проверить
docker-compose ps
docker-compose logs -f
```

## Быстрая команда для обновления на хостинге

```bash
cd /var/www/qlink && \
git pull origin main && \
docker-compose down && \
docker-compose up -d --build && \
docker exec -it qlink-backend sh -c "cd packages/backend && npx prisma migrate deploy"
```

## Проверка перед коммитом

```bash
# Проверить что всё работает локально
npm run build

# Проверить TypeScript ошибки
cd packages/frontend && npm run build
cd packages/backend && npm run build

# Проверить что .env не добавлен в git
git status | grep .env
# Не должно быть вывода!
```

## Важные файлы для коммита

✅ Должны быть в Git:
- `packages/frontend/src/**` - Frontend код
- `packages/backend/src/**` - Backend код
- `packages/backend/prisma/schema.prisma` - Схема БД
- `docker-compose.yml` - Docker конфигурация
- `Dockerfile.*` - Docker образы
- `*.md` - Документация
- `.gitignore` - Игнорируемые файлы

❌ НЕ должны быть в Git:
- `.env` - Переменные окружения
- `node_modules/` - Зависимости
- `.next/` - Build файлы
- `dist/` - Build файлы
- `*.log` - Логи

## Структура коммита

```
feat: краткое описание (до 50 символов)

Подробное описание изменений:
- Что было сделано
- Почему это было сделано
- Какие проблемы решает

Связанные issue: #123
```

## Типы коммитов

- `feat:` - Новая функциональность
- `fix:` - Исправление бага
- `docs:` - Изменения в документации
- `style:` - Форматирование кода
- `refactor:` - Рефакторинг кода
- `test:` - Добавление тестов
- `chore:` - Обновление зависимостей и т.д.
